# direnv integration for automatic nix-shell activation
# Install direnv and add 'eval "$(direnv hook bash)"' to your ~/.bashrc
# Then run 'direnv allow' in this directory

# For flake users (recommended):
if has nix_direnv_version || has nix; then
  use flake
  # Or use flake .#minimal for CLI-only development
fi

# Fallback for non-flake users:
# use nix

# Optional: Watch specific files for changes
watch_file flake.nix
